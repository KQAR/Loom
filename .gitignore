# Tuist generated
*.xcodeproj
*.xcworkspace
Tuist/.build/
.tuist-derived/
Derived/

# Xcode / SwiftPM
DerivedData/
.build/
# Root SPM manifest is a library-reuse surface; Tuist owns the app's dep lock.
/Package.resolved
xcuserdata/
*.xcuserstate

# macOS
.DS_Store
