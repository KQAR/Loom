# Tuist generated
*.xcodeproj
*.xcworkspace
Tuist/.build/
.tuist-derived/
Derived/

# Xcode / SwiftPM
DerivedData/
.build/
# Root SPM manifest is a library-reuse surface; Tuist owns the app's dep lock.
/Package.resolved
xcuserdata/
*.xcuserstate

# macOS
.DS_Store

# Claude Code — per-machine permission allowlist only. The skills under
# .claude/skills are project docs (CLAUDE.md delegates the release and
# engine-embedding procedures to them), so they stay tracked.
.claude/settings.local.json

# Working notes from audit/analysis runs. Findings worth keeping belong in a PR
# description or an issue, where they sit next to the change that resolved them;
# a snapshot of "what was wrong on 2026-07-29" rots in the tree. Ignored because
# a `git add -A` swept one such batch in by accident (#127).
scratch/
